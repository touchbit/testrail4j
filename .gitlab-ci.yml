stages:
- clean
- build
- test
- verify
- deploy

variables:
  SONAR_ARGS: "-Dsonar.gitlab.commit_sha=${CI_COMMIT_SHA} -Dsonar.gitlab.ref_name=${CI_COMMIT_REF_NAME}"

Build modules:
  stage: build
  tags:
    - touchbit-shell
  script:
    # do not use -Dmaven.test.skip=true
    - make build
    - mv testrail4j-integration-tests/target/Corvus.jar Corvus.jar
  artifacts:
    paths:
      - Corvus.jar
    expire_in: 30 days

JUnit:
  stage: test
  tags:
    - touchbit-shell
  script:
    - mvn test
  artifacts:
    when: always
    expire_in: 30 day
    paths:
      - "*/target/reports"

Integration:
  dependencies:
    - Build modules
  stage: test
  tags:
    - touchbit-shell
  variables:
    ARTIFACTS_URL: "${CI_PROJECT_URL}/-/jobs/$CI_JOB_ID/artifacts/file/"
  before_script:
    - docker login -u gitlab-ci-token -p ${CI_JOB_TOKEN} ${CI_REGISTRY}
    - export TR_HTTP_PORT=$(shuf -i10000-60000 -n1)
    - export TR_HTTPS_PORT=$(shuf -i10000-60000 -n1)
    - mkdir ${CI_JOB_ID}
    - cp .indirect/docker-compose.yml ${CI_JOB_ID}/docker-compose.yml
  script:
    - make docker-up
    - java -jar Corvus.jar --artifacts-url ${ARTIFACTS_URL} --http-port ${TR_HTTP_PORT} --https-port ${TR_HTTPS_PORT}
  after_script:
    - make docker-logs
    - make docker-kill
  artifacts:
    when: always
    paths:
      - logs
    expire_in: 30 days

Sonar review:
  stage: verify
  dependencies:
    - JUnit
  tags:
    - touchbit-shell
  except:
    - tags
  script:
    - >
      if [ "$CI_BUILD_REF_NAME" == "master" ]; then
        mvn compile sonar:sonar -Dsonar.login=$SONAR_LOGIN $SONAR_ARGS
      else
        mvn compile sonar:sonar -Dsonar.login=$SONAR_LOGIN $SONAR_ARGS -Dsonar.analysis.mode=preview
      fi

Deploy Snapshot:
  stage: deploy
  tags:
    - touchbit-shell
  except:
    - tags
  when: manual
  script:
    - TR4J_VERSION=$(mvn -q -Dexec.executable=echo -Dexec.args='${project.version}' --non-recursive exec:exec)
    - SNAPSHOT_TR4J_VERSION=${TR4J_VERSION}-SNAPSHOT
    - make ver ${SNAPSHOT_TR4J_VERSION}
    - make deploy
    - "curl -X POST \"https://api.telegram.org/bot$BABBLER_TOKEN/sendMessage?chat_id=$TELEGRAM_TOUCHBIT_CI_CD_CHAT_ID&disable_notification=true&parse_mode=Markdown&text=*TestRail4J*%0ANew version uploaded: %5B${SNAPSHOT_TR4J_VERSION}%5D%28https://touchbit.org/repository/org/touchbit/testrail4j/testrail4j/${SNAPSHOT_TR4J_VERSION}/%29\""

Deploy TestRail4J:
  stage: deploy
  tags:
    - touchbit-shell
  only:
    - tags
  script:
    - make ver $CI_COMMIT_TAG
    - make deploy
    - "curl -X POST \"https://api.telegram.org/bot$BABBLER_TOKEN/sendMessage?chat_id=$TELEGRAM_TOUCHBIT_CI_CD_CHAT_ID&disable_notification=false&parse_mode=Markdown&text=*TestRail4J*%0ANew version deployed: %5B${CI_COMMIT_TAG}%5D%28https://touchbit.org/repository/org/touchbit/testrail4j/testrail4j/${CI_COMMIT_TAG}/%29\""

Clean runner:
  stage: clean
  only:
    - schedules
  tags:
    - touchbit-shell
  script:
    - make docker-clean